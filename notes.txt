FLOW
1. Generate photobooth id/lobby 
    > theres a host and participant?? 
    > sync webcams (left person who opened)

VARIABLES:
a. WebRTC - Responsible for video and audio sharing across internet
b. Signaling Server - Firebase (live database watch if not available use traditional DBs with websocket for event driven)
c. Peer to peer connection - Connection among all the devices (if video conferencing app)
d. STUN ??
e. Local device - Device that offers the connectivity (generated by firebase)
f. Remote device - Answer the connectivity offered

FLOW WITH VARIABLES
1. Create room (as local device e.g. ME)
    > Behind the scene will create the signaling server and the connectivity
    > Local device will generate media stream object from their webcam 
2. 